# Werkzeugkiste: 1337 (Werkzeuge gitignored)

Dieselbe Form wie `base`/`advanced` (siehe deren README), aber die
**Werkzeugdateien** (`*.json`) liegen nicht im Repositorium: Sie stehen in
`.gitignore` und bleiben lokal. Nur dieser README bleibt.

⛔️ Nur defensive und autorisierte Sicherheitswerkzeuge, jedes fragt im
`manual mode` vor dem Wirken nach. Kein Zerstoererisches, keine fremden
Ziele ohne Auftrag.
